import styled from 'styled-components';

export const ProfileWrapper = styled.section`
  margin-top: 2rem;
  border-bottom: 1px solid gray;
  padding-bottom: 2rem;
  color: #8899a6;
  display: flex;
  flex-direction: column;
`;

export const ProfileTitle = styled.small`
  font-size: 1rem;
  display: block;
  margin-top: 0.5rem;
`;

export const ProfileNome = styled.h2`
  font-size: 1.6rem;
  color: #21afe5;
  font-weight: 900;
`;
